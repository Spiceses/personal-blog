import { AppBar, Avatar } from "@mui/material";

const Navbar = () => {
  return (
    <AppBar position="static" color="inherit">
      <Avatar>H</Avatar>
    </AppBar>
  );
};

export default Navbar;
